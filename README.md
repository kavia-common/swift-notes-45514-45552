# Swift Notes

A simple modern notes application built with Next.js. Navigate to [http://localhost:3000](http://localhost:3000) after running `npm install && npm run dev` in `frontend/` to start taking notes. All data is stored locally in your browser.